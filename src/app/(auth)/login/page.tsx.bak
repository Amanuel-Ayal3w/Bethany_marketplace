// This page is deprecated and redirects to the new login page
import { redirect } from 'next/navigation';

export default function DeprecatedLoginPage() {
  // Redirect to the new login page
  redirect('/login');
}
